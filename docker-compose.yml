version: '3.8'

services:
  postgresql:
    image: postgres:16-alpine
    container_name: boltline_postgres
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-boltline}
      POSTGRES_USER: ${POSTGRES_USER:-root}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-password}
    ports:
      - '${POSTGRES_PORT:-5432}:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${POSTGRES_USER:-root}']
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - boltline_network
    restart: unless-stopped

  # Optional: Redis for future caching layer
  # redis:
  #   image: redis:7-alpine
  #   container_name: boltline_redis
  #   ports:
  #     - '6379:6379'
  #   volumes:
  #     - redis_data:/data
  #   healthcheck:
  #     test: ['CMD', 'redis-cli', 'ping']
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5
  #   networks:
  #     - boltline_network
  #   restart: unless-stopped

  # Optional: Backend services for containerized deployment
  # backend-graphql:
  #   build: ./backend-graphql
  #   container_name: boltline_graphql
  #   environment:
  #     DATABASE_URL: ${DATABASE_URL}
  #     EXPRESS_EVENT_SECRET: ${EXPRESS_EVENT_SECRET:-dev-event-secret-12345}
  #     EXPRESS_EVENT_URL: http://backend-express:5000/events/emit
  #   ports:
  #     - '4000:4000'
  #   depends_on:
  #     postgresql:
  #       condition: service_healthy
  #   networks:
  #     - boltline_network
  #
  # backend-express:
  #   build: ./backend-express
  #   container_name: boltline_express
  #   environment:
  #     EXPRESS_EVENT_SECRET: ${EXPRESS_EVENT_SECRET:-dev-event-secret-12345}
  #   ports:
  #     - '5000:5000'
  #   networks:
  #     - boltline_network

volumes:
  postgres_data:
  # redis_data:

networks:
  boltline_network:
    driver: bridge
